# Bearer token sent as `Authorization: Bearer <token>` on every API call.
# Matches the backend's CHEF_API_TOKEN (default: chef-dev-token).
VITE_CHEF_API_TOKEN=chef-dev-token

# Where `npm run dev` proxies `/api` to (the FastAPI backend).
CHEF_BACKEND_URL=http://localhost:8000
